# de-omarchy UI-layer packages — curated from Omarchy's base list, keeping ONLY
# what the desktop UI needs. No kernels, drivers, bootloaders, printers, docker
# or distro-specific packages: your CachyOS/Arch system layer stays untouched.
# The installer installs only the ones that are missing (pacman, then AUR helper).
#
# Format: one package per line; lines starting with # are comments.

# Compositor & portals (usually already installed on Hyprland systems)
hyprland
hyprland-guiutils
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk
qt6-wayland
gtk4-layer-shell

# Desktop shell runtime (repo build — tracks Qt, no AUR rebuilds)
quickshell

# Lock / idle / color temperature
hyprlock
hypridle
hyprsunset
hyprpicker

# Capture / clipboard
grim
slurp
wl-clipboard
cliphist
gpu-screen-recorder
imagemagick
tesseract
tesseract-data-eng

# Audio / media keys / brightness
pamixer
playerctl
brightnessctl
ddcutil

# System integration
networkmanager
bluez-utils
udiskie
power-profiles-daemon
gvfs-mtp
gvfs-nfs
gvfs-smb

# Theming foundations (pure Arch — no GNOME/Yaru)
noto-fonts
noto-fonts-cjk
noto-fonts-emoji
woff2-font-awesome
ttf-jetbrains-mono-nerd

# CLI helpers used by omarchy-* commands
# mise — tool-version manager behind `omarchy default agent <name>` installs
# (claude/codex/grok/… via its npm/github backends). Repo package everywhere.
mise
jq
socat
gum
fzf
fd
bat
eza
fastfetch
libnotify
python-gobject
lazydocker
papirus-icon-theme
# XDG default-terminal spec implementation — Arch/CachyOS repos don't carry
# it; comes from the AUR (install.sh falls back to the AUR helper per-package).
xdg-terminal-exec
# Effects engine behind the animated Omarchy screensaver. Upstream's own repo
# ships a Rust port named `ttfx`; this package provides the original engine as
# `tte` with identical options — omarchy-screensaver accepts either name.
python-terminaltexteffects
